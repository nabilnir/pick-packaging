"use client";

import React from 'react';

/**
 * ErrorReporter Component
 * 
 * A simple placeholder component to prevent layout breakage.
 * In a real application, this could handle client-side error logging.
 */
const ErrorReporter: React.FC = () => {
    return null;
};

export default ErrorReporter;
